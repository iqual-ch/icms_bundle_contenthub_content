# ICMS Recipe: Content Hub Default Content

## Install command
- Install recipe:
  ```bash
  composer require iqual/icms_bundle_contenthub_content && drush recipe ../packages/icms_bundle_contenthub_content
  ```
